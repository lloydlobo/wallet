export type Expense = {
  name: string;
  amount: number;
  id: number;
  description: string;
  is_cash: boolean;
  owner: string;
};
// export type Data = { count: number; next: null; previous: null; results: Expense[] };

